namespace Vam.Client;

/// <summary>The window. It holds a web view and nothing else.</summary>
public partial class MainPage : ContentPage
{
    /// <summary>Builds it.</summary>
    public MainPage() => InitializeComponent();
}
