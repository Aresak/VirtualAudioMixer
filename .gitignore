# Build output
[Bb]in/
[Oo]bj/
[Oo]ut/
[Ll]og/
[Ll]ogs/
artifacts/
publish/
*.binlog

# .NET / Visual Studio / Rider
.vs/
.vscode/
!.vscode/extensions.json
.idea/
*.user
*.suo
*.userprefs
*.sln.docstates
*.DotSettings.user
project.lock.json
project.fragment.lock.json

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/
!packages/build/

# Native build output for the P/Invoke dependencies
native/**/build/
native/**/*.o
native/**/*.obj
*.pdb
*.ilk
*.exp

# MAUI / mobile
*.keystore
*.jks
*.mobileprovision
*.p12
Platforms/Android/**/bin/
Platforms/iOS/**/bin/

# Local configuration and captures — never commit a real session
appsettings.*.local.json
config.local.json
*.local.env
recordings/
sessions/
# Anchored to the repository root. Unanchored, this also matches any source folder called
# Diagnostics/ - and git matches case-insensitively on Windows and case-sensitively on Linux, so
# the same file would be committed on one machine and invisible on the other.
/diagnostics/
*.wav
*.flac
*.mp3
*.m4a

# OS
.DS_Store
Thumbs.db
desktop.ini
